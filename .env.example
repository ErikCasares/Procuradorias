# Copie para .env e preencha. O .env é lido automaticamente pelo docker compose.

# Chave da OpenAI — usada pelo Agente 1 (gpt-4o-mini)
OPENAI_API_KEY=sk-...

# Fuso horário dos timestamps nos relatórios e nomes de arquivo
TZ=America/Campo_Grande

# Porta da interface web (o Easypanel espera 3000)
PORT=3000

# ── Autenticação — SEM ISTO O SERVIÇO NÃO ABRE (falha fechada) ──

# Tokens da API, no formato "rotulo:token" separados por vírgula.
# O rótulo identifica o consumidor no log e no registro do lote.
# Gere cada token com: python -c "import secrets; print(secrets.token_urlsafe(32))"
API_TOKENS=siap:troque-por-um-token-gerado

# Senha do painel do procurador
SENHA_PAINEL=troque-por-uma-senha-forte

# Marque a sessão como Secure (só trafega em HTTPS). Ative em produção.
COOKIE_SEGURO=1

# Horas até a sessão do painel expirar
HORAS_SESSAO=12

# Tamanho máximo de um lote, em MB
MAX_MB_LOTE=200
