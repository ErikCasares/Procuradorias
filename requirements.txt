# Versões fixadas nas mesmas que estão instaladas na máquina de desenvolvimento,
# para que o container reproduza o comportamento já validado localmente.

# Agentes
# A OpenAI saiu na v8.0 do Agente 1: a extração passou a ser 100% determinística
# (regex + OCR), sem chamada a modelo. Nenhum módulo importa 'openai'.
pandas==2.3.3
pdfplumber==0.11.7
pdf2image==1.17.0
pytesseract==0.3.13
openpyxl==3.1.5
pillow==11.3.0

# Interface web
fastapi==0.118.0
uvicorn[standard]==0.37.0
python-multipart==0.0.20
